node
{
     stage("triggerscan"){
    //withCredentials([usernameColonPassword(credentialsId: 'TWISTLOCK_USER', variable: 'TWISTLOCK_PASSWORD')])
    {
        sh '''
        echo 'Testing Multibranch'
        '''
     }
      // echo "$TWISTLOCK_USER"
       // echo "$TWISTLOCK_PASSWORD"
        // curl -s -u "$TWISTLOCK_USER":"$TWISTLOCK_PASSWORD" ${TWISTLOCK_ADDRESS} > twistcli; chmod a+x twistcli
}
}
